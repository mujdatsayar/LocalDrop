# Windows release packages

Windows installer and portable packages are published on the [latest GitHub release](https://github.com/mujdatsayar/LocalDrop/releases/latest).

This local directory is the output location used by the Windows build process. Generated `.exe` and `.zip` files are excluded from Git history to avoid repository size limits.
