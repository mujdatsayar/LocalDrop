# macOS release packages

macOS DMG and portable ZIP packages are published on the [latest GitHub release](https://github.com/mujdatsayar/LocalDrop/releases/latest).

This local directory is the output location used by the macOS build process. Generated `.dmg` and `.zip` files are excluded from Git history to avoid repository size limits.
